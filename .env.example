# Mastodon server configuration
MASTODON_HOST=https://your-mastodon-instance.social
MASTODON_API_TOKEN=your_api_token_here

# Optional: Custom URL for disposable domains list
# If not set, uses the default from disposable-email-domains repo
#DISPOSABLE_DOMAINS_URL=https://raw.githubusercontent.com/disposable-email-domains/disposable-email-domains/master/disposable_email_blocklist.conf

# Optional: Dry run mode (true/false)
# When true, shows what would be done without making changes
DRY_RUN=false
